---
title: Oceanic Guide
template: splash
hero:
  tagline: A Node.js library for interfacing with Discord
  image:
    file: ../../assets/Iso.svg
  actions:
    - text: Introduction
      link: /home/introduction/
      icon: right-arrow
      variant: primary
---

import { Card, CardGrid } from "@astrojs/starlight/components";

## Why Oceanic.js?

<CardGrid>
  <Card title="Easy to Use" icon="approve-check-circle">
    Oceanic.js is an easy-to-use library for your Discord Applications
  </Card>
  <Card title="Latest Discord updates" icon="rocket">
    Oceanic.js is a library that is kept up to date with the latest Discord API
    updates
  </Card>
</CardGrid>

## Example Discord Bot

```js title="index.js" frame="code"
require("dotenv").config();

const { Client } = require("oceanic.js");
const client = new Client({
  auth: `Bot ${process.env.DISCORD_TOKEN}`,
  gateway: {
    intents: ["GUILDS"],
  },
});

client.connect();

client.on("ready", () => {
  console.log(`${client.user.username} has been connected to Discord!`);
});

client.on("error", (error) => {
  console.error(error);
});
```
